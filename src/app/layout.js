export default function Layout({ children }) {
  return (
      <html lang="en">
          <head>
              <title>Konversi Suhu</title>
          </head>
          <body>{children}</body>
      </html>
  );
}
